// Routes
import { elements } from '../shared/dom.js';
import {
    showAuthState,
    showLandingState
} from '../shared/ui.js';

// Define application routes
export const PAGE_ROUTES = {
    home: '/',
    dashboard: '/dashboard',
    projects: '/project',
    account: '/account',
}

